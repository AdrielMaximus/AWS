# Configuração global de autorização para permitir acesso público durante testes
#input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!

type ShoppingListItem @model @auth(rules: [{allow: owner}]) {
  id: ID!
  itemName: String!
}
